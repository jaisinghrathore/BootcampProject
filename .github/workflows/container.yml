name: deploy-container

on:
  push:
    branches: 
      - main
    paths:
      - "package/container/**"

defaults:
  run:
    working-directory: package/container

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - run: npm install
      - run: npm run build

      - uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: AKIAT6E2RMHO4MDP34WK
          aws-secret-access-key: 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
          aws-region: ap-south-1
      - run: aws s3 sync dist s3://accenturefinalproject/container/latest
